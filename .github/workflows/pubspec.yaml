name: nothing_gallery
description: "A minimalist Nothing OS vertical offline gallery."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  photo_manager: ^3.0.0
  permission_handler: ^11.3.0
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
